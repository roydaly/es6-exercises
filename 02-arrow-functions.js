// Rewrite this function as an arrow function
// It should still return the same value

// function addThreeNumbers(a, b, c) {
//   return a + b + c;
// }


// console.log( addThreeNumbers(1, 2, 3) );

var addThreeNumbers = (a,b,c) =>  a + b + c;

console.log( addThreeNumbers(1, 2, 3) );